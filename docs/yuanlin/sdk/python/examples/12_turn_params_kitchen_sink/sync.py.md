# `sync.py` -- Turn 参数全展示示例（同步版）

演示 turn 启动参数的全面用法，包括 `approval_policy`（审批策略，设为 "never"）、`output_schema`（JSON 结构化输出 schema）、`personality`（人格类型，设为 pragmatic）和 `summary`（推理摘要，设为 concise）。请求模型返回符合指定 schema 的 JSON 结构化数据，并验证返回结果的类型正确性。
