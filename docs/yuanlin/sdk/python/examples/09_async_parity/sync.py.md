# `sync.py` -- 异步对等验证示例（同步版）

使用同步 `Codex` 客户端执行与异步示例相同的基本流程，验证同步 API 与异步 API 的行为一致性。创建线程、启动 turn、读取持久化数据，并输出线程 ID、turn ID 和助手文本。此示例无对应的 `async.py`，因为异步对等行为已由 `01_quickstart_constructor/async.py` 覆盖。
