# `sync.py` -- 线程生命周期与控制操作示例（同步版）

演示线程的完整生命周期管理，包括创建、恢复、列出、命名、归档、取消归档、分叉和压缩等操作。该示例覆盖了 `Codex` 的 `thread_start()`、`thread_resume()`、`thread_list()`、`thread_archive()`、`thread_unarchive()`、`thread_fork()` 以及 `Thread.set_name()`、`Thread.compact()` 等全部线程管理 API。
