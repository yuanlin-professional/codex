# `conftest.py` -- 测试模块

## 测试覆盖范围

pytest 配置文件，负责测试环境的路径初始化。将 `sdk/python/src` 添加到 `sys.path` 最前面，并清除已加载的 `codex_app_server` 模块缓存，确保测试总是使用本地源码而非已安装的包。

## 测试场景列表

| 测试函数 | 场景描述 |
|----------|----------|
| （无测试函数） | 本文件为 pytest conftest，仅执行路径和模块缓存的初始化逻辑 |
