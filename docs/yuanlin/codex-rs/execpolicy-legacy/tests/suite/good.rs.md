# `good.rs` — 测试模块

## 测试覆盖范围

验证默认策略中定义的所有正例（`should_match` 列表）确实都能通过策略匹配。

## 测试场景列表

| 测试函数 | 场景描述 |
|----------|----------|
| `verify_everything_in_good_list_is_allowed` | 加载默认策略，对每个正例执行检查，确认所有正例均通过策略匹配 |
