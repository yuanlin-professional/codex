# `bad.rs` — 测试模块

## 测试覆盖范围

验证默认策略中定义的所有反例（`should_not_match` 列表）确实都被策略拒绝。

## 测试场景列表

| 测试函数 | 场景描述 |
|----------|----------|
| `verify_everything_in_bad_list_is_rejected` | 加载默认策略，对每个反例执行检查，确认所有反例均未通过策略匹配 |
