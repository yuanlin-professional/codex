# `lib.rs` — 模糊文件搜索引擎

## 功能概述

高性能的模糊文件名匹配引擎。基于目录遍历构建文件索引，支持多线程搜索（默认 2 线程）、模式排除、匹配索引计算和 JSON 输出。使用评分机制对搜索结果排序。

## 依赖关系

- **引用的 crate**: `rayon`、`ignore`（gitignore 支持）
- **被引用方**: `file-search/src/main.rs`、TUI 文件选择器
