# app-server-protocol/schema/typescript — TypeScript 类型定义

## 功能概述
app-server-protocol 协议的 TypeScript 类型定义文件。包含客户端通知、请求、响应等类型。

## 目录说明
包含大量自动生成的 .ts 类型定义文件，如 ClientNotification.ts、ClientRequest.ts、ServerNotification.ts 等。
