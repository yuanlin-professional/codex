# app-server-protocol/schema/json/v2 — V2 JSON Schema

## 功能概述
app-server-protocol V2 版本的 JSON Schema 定义。包含更丰富的协议类型。

## 目录说明
包含 V2 版本的 JSON Schema 文件，如 CommandExecParams.json、ConfigReadParams.json 等。
