# `popup_consts.rs` -- 弹出窗口共享常量

`popup_consts.rs` 定义了底部面板弹出窗口的共享常量和辅助函数。`MAX_POPUP_ROWS`（值为 8）限定所有弹出窗口的最大显示行数，保持统一视觉体验。`standard_popup_hint_line()` 生成标准的页脚提示行（"Press Enter to confirm or Esc to go back"）。
