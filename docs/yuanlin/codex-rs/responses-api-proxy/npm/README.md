# responses-api-proxy/npm - npm 发布包

## 功能概述

此目录包含 Responses API Proxy 的 npm 包配置和启动脚本，用于将 Rust 编译的二进制文件以 npm 包形式分发。

## 目录结构

| 文件/目录 | 说明 |
|-----------|------|
| `package.json` | npm 包配置 |
| `README.md` | 包使用说明 |
| `bin/` | 可执行入口脚本 |
