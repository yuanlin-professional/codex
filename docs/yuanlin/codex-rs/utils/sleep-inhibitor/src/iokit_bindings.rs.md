# `iokit_bindings.rs` — macOS IOKit 电源管理 FFI 绑定

由 `rust-bindgen 0.71.1` 自动生成的 macOS IOKit 电源管理 API 绑定。定义了 `IOPMAssertionCreateWithName`（创建电源断言）和 `IOPMAssertionRelease`（释放电源断言）的 FFI 函数声明，以及相关类型常量（`kIOReturnSuccess`、`kIOPMAssertionLevelOn` 等）。
