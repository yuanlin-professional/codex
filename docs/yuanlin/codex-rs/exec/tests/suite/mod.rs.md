# `mod.rs` — 测试模块

## 测试覆盖范围

exec 集成测试套件的模块聚合文件，引入所有子测试模块。

## 测试场景列表

| 测试函数 | 场景描述 |
|----------|----------|
| (模块聚合) | 引入 add_dir、apply_patch、auth_env、ephemeral、mcp_required_exit、originator、output_schema、prompt_stdin、resume、sandbox、server_error_exit |
