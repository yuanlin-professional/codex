# codex-cli/bin - CLI 可执行文件入口

## 功能概述

此目录包含 Codex CLI 的可执行入口脚本，是用户直接调用的命令行工具入口点。

## 目录结构

| 文件 | 说明 |
|------|------|
| `codex.js` | Codex CLI 的主入口脚本（Node.js） |
| `rg` | 内置的 ripgrep 二进制文件，用于快速代码搜索 |
